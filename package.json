{
  "name": "json_server",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve-json": "json-server --watch db.json --port 3000 --routes routes.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "JSON Server API for testing and development",
  "dependencies": {
    "axios": "^1.13.5",
    "json-server": "^0.17.4"
  }
}
